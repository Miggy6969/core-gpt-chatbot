// routes/auth.js
const express = require('express');
const router = express.Router();

// Placeholder for FB login + long-lived token exchange
router.get('/', (req, res) => {
  res.send('OAuth logic coming soon!');
});

module.exports = router;